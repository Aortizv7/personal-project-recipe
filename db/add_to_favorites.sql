/* on this add to favorites function i must do a join 
and an alter table 
 
where the recipe_id is pushed from the user to match the user's id 
so i need to join the users table with the column of id and the 
favorites table with the user_id matches 
it will need to be an insert into 
*/

